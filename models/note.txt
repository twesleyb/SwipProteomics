# Making the gif.

# In pymol, type the following commands:
# mset 1,180 
# util.mroll 1, 180, 1
# set ray_trace_frames, 1 
# set cache_frames, 0

# Then save as a video. File > save as video > PNG images

# Convert the images to a gif with img2gif.

## STOP ##
# Don't use Pymol! It's full use is restricted.

## Use Chimera instead!
# Convert .pse (pymol specific format) to .pdb.
# In Chimera, record a video:
# 
# Convert the video to a stack  of images.
# ffmpeg -i infile.avi -f image2 image-%03d.jpg

# Convert the images to a gif.
# img2gif JPG/ 1 -1 wt.gif
